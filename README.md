# `@sanedefaults/prettier-config`

> A Sane default [Prettier](https://prettier.io) config.

## Usage

**Install**:

```bash
$ yarn add --dev @sanedefaults/prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@sanedefaults/prettier-config"
}
```
